export type OnChangeInputType = (
  event: React.ChangeEvent<HTMLInputElement>
) => void;
export type OnChangeSelectType = (
  event: React.ChangeEvent<HTMLSelectElement>
) => void;

export const formatTypeArray = ["png", "jpg", "jpeg", "webp", "gif"];
